# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# Backend configuration - relative paths work in both development and production
NEXT_PUBLIC_BACKEND_API_URL=/v1
NEXT_PUBLIC_BACKEND_WS_URL=/v1/stream

# For standalone frontend development, use full URLs:
# NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000/v1
# NEXT_PUBLIC_BACKEND_WS_URL=ws://localhost:8000/v1/stream

# For production, use your actual domain:
# NEXT_PUBLIC_BACKEND_API_URL=https://api.yourdomain.com/v1
# NEXT_PUBLIC_BACKEND_WS_URL=wss://api.yourdomain.com/v1/stream
