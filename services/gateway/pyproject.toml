[project]
name = "sara-gateway"
version = "0.0.0"
description = "FastAPI edge service for Sara"
dependencies = []          # runtime deps already baked into the Docker image
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
